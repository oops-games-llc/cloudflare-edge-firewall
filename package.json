{
  "name": "conversion-business-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Edge API Gateway Firewall for Conversion.Business. Zero-telemetry, GDPR compliant CAPTCHA protection.",
  "keywords": ["captcha", "cloudflare-worker", "gdpr-captcha", "zero-telemetry", "eprivacy-compliant", "bot-protection"],
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vitest": "^1.0.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
